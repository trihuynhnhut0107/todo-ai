import { View, Text } from 'react-native'
import React from 'react'
import { Redirect } from 'expo-router'

const chat_holder = () => {
  return <Redirect href="/(main)/chat"  />
  
}

export default chat_holder